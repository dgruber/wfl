package statistics

/*
type Statisticer interface {
	// Add adds a value to the statistic.
	AddSubmissions(value int, annotation string) error
}

type DefaultStatisticer struct {
	all map[string]int
}

func NewDefaultStatistics() Statisticer {
	return &DefaultStatisticer{
		all: make(map[string]int),
	}
}

func (d *DefaultStatisticer) AddSubmissions(amount int, annotation string) error {
	d.all[annotation] += amount
	return nil
}
*/
